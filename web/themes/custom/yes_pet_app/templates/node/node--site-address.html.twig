<div class="title__local" id="title__local">
  {{ node.title.value }}
</div>

<div class="owl-carousel owl-theme">
  {% for item in content.field_image_location['#items'] %}
    {% set image_url = file_url(item.entity.uri.value) %}
    <div class="owl__field-item">
      <img class="owl__image" src="{{ image_url }}">
      {# <div class="image-caption">{{ item.alt }}</div> #}
    </div>
  {% endfor %}
</div>

{% if user.isAnonymous == FALSE %}
  <div class="group-rating" id="group-rating">
    {# {{ content.field_rating }} #}
  
    {{ content.vote_vote_field }}
  
  </div>
  {% else %}
    <div class="not__logedin_rating" id="not__logedin_rating">
      <a href="#">{{"Login to rating this place"|t}}</a>
    </div>
{% endif %}

{# <div class="phone__group">

  <div class="whatsapp__ico">
    <img class="whatsapp__link" src="{{ file_url('themes/custom/yes_pet_app/images/whatsappIconmini.png') }}">
  </div>

  {{ content.field_phone }}

  {{content.field_ph}}

</div> #}

<div class="social__group">

  <span class="whatsapp"><div class="ico__value ico__whatsapp">{{ content.field_ph['#items'].0.value }}</div></span>

  <span class="instagram"><div class="ico__value ico__instagram">{{ content.field_instagram['#items'].0.value }}</div></span>

  <span class="facebook"><div class="ico__value ico__facebook">{{ content.field_facebook['#items'].0.value }}</div></span>

  <span class="website"><div class="ico__value ico__website">{{ content.field_web_site['#items'].0.value }}</div></span>

</div>


<div class="location__description">
  {{ content.body }}
</div>

{{ content.field_complete_address }}

{{ content.field_comments }}
